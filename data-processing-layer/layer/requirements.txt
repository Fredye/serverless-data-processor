# Runtime dependencies packaged into the Lambda Layer.
# Keep these versions pinned so every deployment has reproducible artifacts.
boto3==1.34.162
aws-lambda-powertools==2.43.1
psycopg[binary,pool]==3.2.1
